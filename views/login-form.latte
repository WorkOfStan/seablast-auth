<div n:if="$showLogin">
    {* Show form with email and CSRF *}
    <form method="POST">
        <input type="email" name="email" placeholder="vas@email.cz" autofocus="true"/>
        <input type="hidden" value="{$csrfToken}" name="csrfToken"/>
        <input type="submit" value="Ok">
    </form>
</div>
{* class="g_id_signout" should prevent an automatic One Tap relogin *}
<p n:if="$showLogout"><a href="{$configuration->getString('SB_APP_ROOT_ABSOLUTE_URL')}{$configuration->getString('AuthApp:USER_ROUTE')}/?logout" class="g_id_signout" rel="nofollow">Logout</a></p>
<p n:if="$message">{$message}</p>
